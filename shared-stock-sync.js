(function(){
  'use strict';
  const SB=()=>window.barokahSupabase;
  let sharedStockRows=[];
  let sharedOwnerId=null;
  let editingId=null;
  let syncing=false;
  let lastSignature='';

  const $=id=>document.getElementById(id);
  const factor=u=>{const k=String(u||'').trim().toLowerCase();return k==='papan'?30:k==='ikat'?180:1;};
  const fmt=x=>Number(x||0).toLocaleString('id-ID',{maximumFractionDigits:10});
  const esc=v=>String(v==null?'':v).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
  const sort=rows=>rows.slice().sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)||String(a.id).localeCompare(String(b.id)));
  const saldo=rows=>rows.reduce((n,r)=>n+Number(r.delta_butir||0),0);
  const running=rows=>{let n=0;return sort(rows).map(r=>{n+=Number(r.delta_butir||0);return {...r,calculated_saldo_after_butir:n};});};
  async function admin(){try{const r=await SB()?.rpc('is_admin');return !r?.error&&r.data===true;}catch(e){return false;}}
  async function currentUser(){try{const r=await SB()?.auth.getUser();return r?.data?.user||null;}catch(e){return null;}}
  function formIds(type){if(type==='Masuk')return ['stockIn','stockInUnit','stockInNote'];if(type==='Keluar')return ['stockOut','stockOutUnit','stockOutNote'];if(type==='Retak')return ['badEggs','badEggsUnit'];return ['unfitEggs','unfitEggsUnit'];}
  function clearForms(){['stockIn','stockInNote','stockOut','stockOutNote','badEggs','unfitEggs'].forEach(id=>{const e=$(id);if(e)e.value='';});}
  function findMovementForm(type){const id=formIds(type)[0],input=$(id);if(!input)return null;return input.closest('.card,.form-card,.grid-2,.grid-3,section')||input.parentElement;}
  function ensureEditUI(type,row){let box=$('stockEditControls');if(!box){box=document.createElement('div');box.id='stockEditControls';box.style.cssText='display:none;margin:0 0 14px;padding:14px 16px;border:1px solid #e6c75a;border-radius:14px;background:#fff8d9;color:#5c4a00;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;';box.innerHTML='<div><b id="stockEditControlsTitle">✏️ Mode Edit Stok</b><div id="stockEditControlsText" style="font-size:12px;margin-top:4px;font-weight:600"></div></div><button type="button" id="stockEditControlsCancel" class="btn ghost">Batal Edit</button>';document.body.appendChild(box);$('stockEditControlsCancel').onclick=()=>cancelEdit();}const form=findMovementForm(type);if(form&&form.parentNode&&!box.dataset.placedFor){form.parentNode.insertBefore(box,form);box.dataset.placedFor='1';}box.style.display='flex';$('stockEditControlsTitle').textContent='✏️ Sedang Edit: '+type;$('stockEditControlsText').textContent='Ubah data pada form Pergerakan Stok di bawah, lalu tekan tombol Simpan Perubahan.';return box;}
  function setSaveButton(type,editing){const id=formIds(type)[0],input=$(id);if(!input)return;const form=input.closest('.card,.form-card,.grid-2,.grid-3,section')||input.parentElement;if(!form)return;let btn=form.querySelector('[data-stock-save-edit]');if(editing){if(!btn){btn=document.createElement('button');btn.type='button';btn.className='btn primary';btn.dataset.stockSaveEdit='1';btn.style.cssText='width:100%;margin-top:10px;font-size:13px;min-height:46px;';form.appendChild(btn);}btn.textContent='💾 Simpan Perubahan';btn.style.display='block';btn.onclick=()=>saveEditFromForm(type);}else if(btn){btn.style.display='none';}}
  function hideEdit(){const b=$('stockEditControls');if(b)b.style.display='none';['Masuk','Keluar','Retak','Tidak Layak'].forEach(t=>setSaveButton(t,false));clearForms();}
  function populate(row){clearForms();const type=row.movement_type,ids=formIds(type);const q=$(ids[0]),u=$(ids[1]),n=ids[2]?$(ids[2]):null;if(q)q.value=String(row.qty??'');if(u)u.value=String(row.unit??'Butir');if(n)n.value=String(row.note??'');ensureEditUI(type,row);setSaveButton(type,true);q?.scrollIntoView({behavior:'smooth',block:'center'});setTimeout(()=>q?.focus(),300);}
  function beginEdit(id){const row=sharedStockRows.find(r=>String(r.id)===String(id));if(!row)return;editingId=id;populate(row);}
  function cancelEdit(){editingId=null;hideEdit();render();}
  function actionButtons(id){return '<button type="button" class="btn ghost stock-action-edit" data-stock-edit="'+esc(id)+'" title="Edit stok">✏️ Edit</button> <button type="button" class="btn danger stock-action-delete" data-stock-delete="'+esc(id)+'" title="Hapus stok">🗑️ Hapus</button>';}
  function render(){const rows=running(sharedStockRows),n=saldo(sharedStockRows);if($('stockWarehouse'))$('stockWarehouse').textContent=fmt(n)+' Butir';if($('stockAutoPreview'))$('stockAutoPreview').textContent='Saldo saat ini: '+fmt(n)+' Butir. Stok tersimpan di database online.';const bad=rows.filter(r=>r.movement_type==='Retak').reduce((a,r)=>a+Number(r.qty||0)*factor(r.unit),0),unfit=rows.filter(r=>r.movement_type==='Tidak Layak').reduce((a,r)=>a+Number(r.qty||0)*factor(r.unit),0);if($('badEggsInfo'))$('badEggsInfo').textContent='Total Telur Retak: '+fmt(bad)+' Butir';if($('unfitEggsInfo'))$('unfitEggsInfo').textContent='Total Telur Tidak Layak: '+fmt(unfit)+' Butir';const box=$('stockHistory');if(!box)return;if(!rows.length){box.innerHTML='<div class="stock-history-empty">Belum ada riwayat stok.</div>';return;}box.innerHTML='<div class="stock-table-wrap"><table class="stock-history-table"><thead><tr><th>Tanggal</th><th>Produk</th><th>Jenis</th><th>Jumlah</th><th>Konversi</th><th>Saldo Setelah</th><th>Keterangan</th><th>Aksi</th></tr></thead><tbody>'+rows.slice().reverse().map(r=>{const d=Number(r.delta_butir||0),sign=d>=0?'+':'-',a=Math.abs(d),conv=a%30===0?sign+fmt(a/30)+' Papan | '+sign+fmt(a)+' Butir':sign+fmt(a)+' Butir';return '<tr data-stock-row="'+esc(r.id)+'"><td>'+new Date(r.created_at).toLocaleDateString('id-ID')+'</td><td><b>'+esc(r.product||'Telur Ayam Ras')+'</b></td><td><b>'+esc(r.movement_type||'')+'</b></td><td>'+sign+fmt(Math.abs(Number(r.qty)||0))+' '+esc(r.unit||'')+'</td><td>'+conv+'</td><td><b>'+fmt(r.calculated_saldo_after_butir)+' Butir</b></td><td>'+esc(r.note||'-')+'</td><td>'+actionButtons(r.id)+'</td></tr>';}).join('')+'</tbody></table></div>';}
  async function load(){const sb=SB(),u=await currentUser();if(!sb||!u)return;try{const isAdmin=await admin();let q=sb.from('stock_movements').select('*').order('created_at',{ascending:true}).order('id',{ascending:true});if(!isAdmin)q=q.eq('user_id',u.id);const {data,error}=await q;if(error)throw error;const rows=data||[];const signature=rows.map(x=>[x.id,x.updated_at||'',x.created_at,x.movement_type,x.qty,x.unit,x.delta_butir,x.saldo_after_butir,x.note].join('|')).join('||');if(signature===lastSignature)return;lastSignature=signature;sharedStockRows=rows;sharedOwnerId=rows[0]?.user_id||u.id;try{localStorage.setItem('barokah_stock_history_v53',JSON.stringify(sharedStockRows));}catch(e){}render();}catch(e){console.error('Shared stock load:',e);}}
  async function refreshShared(){if(syncing)return;syncing=true;try{await load();}finally{syncing=false;}}
  async function add(type){const sb=SB(),u=await currentUser();if(!sb||!u){alert('Sesi database belum aktif. Silakan login ulang.');return;}if(editingId){await saveEditFromForm(type);return;}let q,unit,note;if(type==='Masuk'){q=parseInputNumber($('stockIn')?.value);unit=$('stockInUnit')?.value.trim();note=$('stockInNote')?.value.trim();}else if(type==='Keluar'){q=parseInputNumber($('stockOut')?.value);unit=$('stockOutUnit')?.value.trim();note=$('stockOutNote')?.value.trim();}else if(type==='Retak'){q=parseInputNumber($('badEggs')?.value);unit=$('badEggsUnit')?.value.trim()||'Butir';note='Telur retak';}else{q=parseInputNumber($('unfitEggs')?.value);unit=$('unfitEggsUnit')?.value.trim()||'Butir';note='Telur tidak layak';}if(!Number.isFinite(q)||q<=0){alert('Masukkan jumlah yang benar.');return;}if(!unit){alert('Isi satuan.');return;}if((type==='Masuk'||type==='Keluar')&&!note){alert('Isi keterangan.');return;}const current=saldo(sharedStockRows),delta=q*factor(unit)*(type==='Masuk'?1:-1);if(type!=='Masuk'&&Math.abs(delta)>current){alert('Stok Gudang tidak mencukupi. Stok saat ini '+fmt(current)+' Butir.');return;}const owner=(await admin())?(sharedOwnerId||u.id):u.id;try{const {data,error}=await sb.from('stock_movements').insert({user_id:owner,product:'Telur Ayam Ras',movement_type:type,qty:q,unit,delta_butir:delta,saldo_after_butir:current+delta,note}).select('*').single();if(error)throw error;sharedStockRows.push(data);lastSignature='';await recalcBalances();render();if(type==='Masuk'){$('stockIn').value='';$('stockInNote').value='';}if(type==='Keluar'){$('stockOut').value='';$('stockOutNote').value='';}if(type==='Retak')$('badEggs').value='';if(type==='Tidak Layak')$('unfitEggs').value='';if(typeof toast==='function')toast('Perubahan stok tersimpan ke database.');}catch(err){alert('Gagal menyimpan stok: '+(err.message||err));}}
  async function saveEditFromForm(type){const sb=SB(),u=await currentUser();if(!sb||!u)return;const row=sharedStockRows.find(r=>String(r.id)===String(editingId));if(!row){alert('Data stok yang diedit tidak ditemukan.');return;}let q,unit,note;if(type==='Masuk'){q=parseInputNumber($('stockIn')?.value);unit=$('stockInUnit')?.value.trim();note=$('stockInNote')?.value.trim();}else if(type==='Keluar'){q=parseInputNumber($('stockOut')?.value);unit=$('stockOutUnit')?.value.trim();note=$('stockOutNote')?.value.trim();}else if(type==='Retak'){q=parseInputNumber($('badEggs')?.value);unit=$('badEggsUnit')?.value.trim()||'Butir';note='Telur retak';}else{q=parseInputNumber($('unfitEggs')?.value);unit=$('unfitEggsUnit')?.value.trim()||'Butir';note='Telur tidak layak';}if(!Number.isFinite(q)||q<=0){alert('Masukkan jumlah yang benar.');return;}if(!unit){alert('Isi satuan.');return;}if((type==='Masuk'||type==='Keluar')&&!note){alert('Isi keterangan.');return;}const delta=q*factor(unit)*(type==='Masuk'?1:-1),others=sharedStockRows.filter(r=>String(r.id)!==String(row.id)),otherSaldo=saldo(others);if(delta<0&&Math.abs(delta)>otherSaldo){alert('Stok Gudang tidak mencukupi untuk perubahan ini. Stok tersedia setelah mengabaikan data yang diedit: '+fmt(otherSaldo)+' Butir.');return;}try{let query=sb.from('stock_movements').update({qty:q,unit,movement_type:type,delta_butir:delta,note}).eq('id',row.id);if(!(await admin()))query=query.eq('user_id',u.id);const {error}=await query;if(error)throw error;Object.assign(row,{qty:q,unit,movement_type:type,delta_butir:delta,note});await recalcBalances();try{localStorage.setItem('barokah_stock_history_v53',JSON.stringify(sharedStockRows));}catch(e){}editingId=null;lastSignature='';hideEdit();render();if(typeof toast==='function')toast('Stok berhasil diedit dan disimpan ke database.');}catch(err){alert('Gagal menyimpan perubahan stok: '+(err.message||err));}}
  async function recalcBalances(){const sb=SB();if(!sb||!sharedStockRows.length)return;const ordered=sort(sharedStockRows);let n=0;const isAdmin=await admin();for(const row of ordered){n+=Number(row.delta_butir||0);if(Number(row.saldo_after_butir||0)!==n){row.saldo_after_butir=n;let q=sb.from('stock_movements').update({saldo_after_butir:n}).eq('id',row.id);if(!isAdmin){const u=await currentUser();q=q.eq('user_id',u.id);}const {error}=await q;if(error)throw error;}}}
  async function remove(id){const sb=SB(),u=await currentUser();if(!sb||!u)return;const row=sharedStockRows.find(r=>String(r.id)===String(id));if(!row)return;if(!confirm('Hapus riwayat stok ini? Data yang dihapus tidak bisa dikembalikan dari aplikasi.'))return;try{let q=sb.from('stock_movements').delete().eq('id',id);if(!(await admin()))q=q.eq('user_id',u.id);const {error}=await q;if(error)throw error;sharedStockRows=sharedStockRows.filter(r=>String(r.id)!==String(id));if(String(editingId)===String(id)){editingId=null;hideEdit();}lastSignature='';await recalcBalances();render();if(typeof toast==='function')toast('Riwayat stok berhasil dihapus.');}catch(err){alert('Gagal menghapus stok: '+(err.message||err));}}
  async function clear(){const sb=SB(),u=await currentUser();if(!sb||!u)return;if(!confirm('Hapus seluruh riwayat stok dan reset Stok Gudang?'))return;try{let q=sb.from('stock_movements').delete();if(!(await admin()))q=q.eq('user_id',u.id);const {error}=await q;if(error)throw error;sharedStockRows=[];editingId=null;lastSignature='';hideEdit();render();alert('Riwayat stok dan saldo stok sudah di-reset ke 0 Butir.');}catch(err){alert('Gagal menghapus riwayat stok: '+(err.message||err));}}
  document.addEventListener('click',e=>{const edit=e.target.closest?.('[data-stock-edit]');if(edit){e.preventDefault();beginEdit(edit.getAttribute('data-stock-edit'));return;}const del=e.target.closest?.('[data-stock-delete]');if(del){e.preventDefault();remove(del.getAttribute('data-stock-delete'));}});
  window.addStockIn=()=>add('Masuk');window.addStockOut=()=>add('Keluar');window.addBadEggs=()=>add('Retak');window.addUnfitEggs=()=>add('Tidak Layak');window.editStockMovement=beginEdit;window.deleteStockMovement=remove;window.clearStockHistory=clear;window.getWarehouseStock=()=>saldo(sharedStockRows);window.renderStock=render;
  const oldSync=window.barokahCloudSync;window.barokahCloudSync=async function(){if(typeof oldSync==='function')await oldSync();await refreshShared();};
  setTimeout(()=>refreshShared(),1200);
  setInterval(()=>refreshShared(),2000);
})();