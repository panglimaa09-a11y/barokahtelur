(function(){'use strict';const SB=()=>window.barokahSupabase,esc=v=>String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c])),fmt=n=>Number(n||0).toLocaleString('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0});let timer=null,running=false;async function getUser(){const sb=SB();if(!sb)throw Error('Supabase belum siap.');const{data,error}=await sb.auth.getUser();if(error)throw error;if(!data.user)throw Error('Sesi login tidak aktif.');return data.user}async function sync(){if(running)return;const page=document.getElementById('page-operational'),box=document.getElementById('opTable');if(!page||!page.classList.contains('active')||!box)return;running=true;try{const u=await getUser(),{data,error}=await SB().from('operational_transactions').select('*').eq('user_id',u.id).order('transaction_date',{ascending:false}).order('created_at',{ascending:false});if(error)throw error;const rows=data||[],ins=rows.filter(x=>x.kind==='pemasukan').reduce((s,x)=>s+Number(x.amount||0),0),outs=rows.filter(x=>x.kind==='pengeluaran').reduce((s,x)=>s+Number(x.amount||0),0);const a=document.getElementById('opTotalIn'),b=document.getElementById('opTotalOut'),c=document.getElementById('opNet'),d=document.getElementById('opCount');if(a)a.textContent=fmt(ins);if(b)b.textContent=fmt(outs);if(c)c.textContent=fmt(ins-outs);if(d)d.textContent=rows.length;const filter=document.getElementById('opFilter')?.value||'semua',q=(document.getElementById('opSearch')?.value||'').toLowerCase().trim();let list=filter==='semua'?rows:rows.filter(x=>x.kind===filter);if(q)list=list.filter(x=>(String(x.description||'')+' '+String(x.reference_no||'')+' '+String(x.category||'')+' '+String(x.note||'')).toLowerCase().includes(q));if(!list.length){box.innerHTML='<div class="op-empty">Belum ada transaksi operasional.</div>';return}box.innerHTML='<table><thead><tr><th>No. Transaksi</th><th>Tanggal</th><th>Jenis</th><th>Kategori</th><th>Keterangan</th><th>Nominal</th><th>Aksi</th></tr></thead><tbody>'+list.map(r=>'<tr><td class="op-ref">'+esc(r.reference_no||'-')+'</td><td>'+esc(r.transaction_date||'-')+'</td><td><span class="'+(r.kind==='pemasukan'?'op-in':'op-out')+'">'+(r.kind==='pemasukan'?'Pemasukan':'Pengeluaran')+'</span></td><td>'+esc(r.category||'-')+'</td><td><strong>'+esc(r.description||'-')+'</strong><span class="sub">'+esc(r.note||'')+'</span></td><td class="'+(r.kind==='pemasukan'?'op-in':'op-out')+'">'+fmt(r.amount)+'</td><td class="op-actions-cell"><button class="op-del" data-op-del="'+esc(r.id)+'">Hapus</button><button class="op-nota" data-op-nota="'+esc(r.id)+'">🧾 Nota</button></td></tr>').join('')+'</tbody></table>';window.dispatchEvent(new CustomEvent('barokah:operational-synced',{detail:{rows}})}catch(e){console.error(e);if(box&&!box.querySelector('table'))box.innerHTML='<div class="op-empty">Gagal memuat riwayat operasional: '+esc(e.message||e)+'</div>'}finally{running=false}}function start(){if(timer)clearInterval(timer);sync();timer=setInterval(sync,5000);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync()});window.addEventListener('barokah:operational-open',sync)}window.BarokahOperationalSync={sync,start};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();